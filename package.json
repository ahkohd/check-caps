{
  "name": "check-caps",
  "version": "1.0.2",
  "description": "Native node module to check if caps lock is on/off (windows, mac os, linux)",
  "main": "index.js",
  "gypfile": true,
  "scripts": {
    "build": "node-gyp rebuild",
    "clean": "node-gyp clean",
    "build-addon": "node-gyp rebuild && npm rebuild"
  },
  "author": "Victor Aremu",
  "keywords": ["node", "caplocks", "native", "windows", "macos", "linux", "status", "cap"],
  "license": "MIT",
  "devDependencies": {
    "node-gyp": "^5.0.3"
  },
  "dependencies": {
    "node-addon-api": "^1.7.1"
  }
}
